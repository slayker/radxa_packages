<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/title"
        style="@style/tam_header_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="0"
        android:text="@string/label_downloading_update" />

    <View
        android:id="@+id/tam__header_divider"
        style="@style/tam__header_divider"
        android:layout_marginTop="6dp" />

    <ProgressBar
        android:id="@+id/progressbar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_weight="0"
        android:indeterminateDrawable="@drawable/tam__progress_indeterminate_horizontal"
        android:indeterminateOnly="false"
        android:progressDrawable="@drawable/tam__progress_horizontal_dark"
        android:visibility="gone" />
    <!-- style="?android:attr/progressBarStyleHorizontal" -->

</LinearLayout>